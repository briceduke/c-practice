#include <stdio.h>
#include <stdlib.h>

int addNumbers(int num1, int num2) {
    return num1 + num2;
}

int main()
{
    // Functions

    // either declare before main function or add a header at the top

    int sum = addNumbers(4, 20);

    return 0;
}
