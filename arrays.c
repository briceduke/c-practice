#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Arrays

    // Can have multi-dimensional arrays

    int luckyNumbers[] = { 4, 8, 15, 16, 23, 42 }; // [] is array definition, but values are in {}

    luckyNumbers[0] = 90;

    return 0;
}
